package processes

//client

import (
	"../mp0/messages"
	"fmt"
	"net"
	"os"
)

func SendMessage(m chan Message, address string) {
	//Connect to port
	c, err := net.Dial("tcp", address)
	if err != nil {
		panic(err)
	}

	// Send Message
	fmt.Println("Sending a message...")
}
