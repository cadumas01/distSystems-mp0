package processes

// server

import (
	"../mp0/messages"
	"fmt"
	"net"
	"os"
)

func StartServer(address string) {
	ln, err := net.Listen("tcp", address)

	if err != nil {
		panic("Error listening")
	}

	defer ln.close() //not sure about

	c.Write("Started server")

}

func ReceiveMessage(address string) {

}
